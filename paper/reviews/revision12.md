# Revision implementation by reviewer 12

Edited only the assigned four Overleaf sources: `supp_theory_stages.tex`, `supp_theory_rules.tex`, `supp_theory_conf.tex`, and `proofs.tex`.

- Replaced the false identification of reflection-augmented ERM with symmetrized-predictor ERM by the correct Jensen upper bound. Retained the valid population test-time symmetry theorem and explicitly separated its exact assumptions from empirical near-mirror checks.
- Removed claims that stacking is safe, free, or incapable of meaningful overfitting. Recast the stage bounds as fixed-candidate conditional capacity results, identified observed maxima as uncertified population bounds, made fixed training-array/configuration assumptions explicit for the composite correction class, and accounted for combining probability bounds.
- Rewrote the margin rule using the binary decision d(x)=1{x<0}, assigning ties to no mix. Supplied the valid tie-inclusive one-sided proof. Removed the inference from pooled empirical standard deviation to a certified conditional sub-Gaussian proxy.
- Distinguished population coordinatewise optima from empirical minimizers and from the reported nonseparable relative-norm metrics; added explicit i.i.d./fixed-weight conditions to empirical selection. Removed stale claims that the deployed reused-validation procedure inherits these guarantees.
- Replaced Proposition S2.5, retaining `prop:aniso`, with a deterministic finite-design rank-truncation theorem: for a radius-R input domain, L-Lipschitz g, and retained rank r, the error is at most ||h||K times the feature-space regularized power function plus LR sigma_(r+1)(1+||c||1). The theorem handles the actual labels G(X), including their deviation from the projected target. Removed the old informal sample-size rates and their entire heuristic proof; supplied a complete short proof.
- Rewrote the conformal section to define an infinite quantile when k=m+1, preserve lower coverage with ties and exact k/(m+1) coverage without ties, and separate exchangeability from the stronger conditional i.i.d.-continuous assumptions needed for the Beta/Beta-binomial law. Retained numerical campaign coverage as retrospective observations compared with a conditional reference law, without claiming that a public-test split certifies independence from model development.
- Fixed the sharpness witness's scaled training row in the proof of Theorem 6.9. Restricted the derivative proof's strict positivity claim to -1<rho<1/t, matching the main-text amendment. Handled the trivial M=1 case before the simplex proof chooses k=m(M-1).
- Added stable `proof:<result-label>` anchors to all separate proof subsections, plus the local margin proof.

Source environment pairs were checked for balance in all four edited files. Root handles compilation and final per-result proof pointers. Reviewer 19 independently audited the new rank-truncation statement and proof and found no required correction, including the singular-Gram/pseudoinverse, repeated projected design points, positive nugget, and full-rank boundaries. No expensive experiments were rerun and no empirical results were fabricated or changed.
